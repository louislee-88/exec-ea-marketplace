---
description: Research a topic, person, or account and produce a cited summary; save durable findings to memory.
disable-model-invocation: true
---

# Research

Research: **"$ARGUMENTS"**

- Use web search + connected sources (HubSpot, email, Read.ai) + existing memory.
- Produce a concise, **cited** summary. **State what you could NOT find** (gaps) — do not invent.
- Write durable findings into the relevant record under `entities/` (create it if new), following `_protocol.md` (stamp source + date + confidence, link related entities with `[[...]]`).
- Report the summary to the executive.
