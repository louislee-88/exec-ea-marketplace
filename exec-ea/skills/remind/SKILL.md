---
description: Create a time-based reminder.
disable-model-invocation: true
---

# Remind

Create a reminder from: **"$ARGUMENTS"** (what + when).

- Log it as an open row in `commitments.md` with the due time.
- When Microsoft To-Do is connected, also create the task there.
- Confirm in one line.
